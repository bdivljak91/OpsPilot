spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5433}/${DB_NAME:opspilot}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

logging:
  level:
    com.opspilot: DEBUG
